import math


def area(r):
    '''
    Calculating circle's square function.
        ARGUMENTS:
            * r (int) - circle's radius
        RETURNS:
            * result square
    '''
    return math.pi * r * r


def perimeter(r):
    '''
    Calculating circle's perimeter function.
        ARGUMENTS:
            * r (int) - circle's radius
        RETURNS:
            * result perimeter
    '''
    return 2 * math.pi * r

